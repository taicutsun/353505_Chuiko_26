version: '3.8'

services:
  web:
    build:
      context: .
    container_name: agencyapp_web
    command: python manage.py runserver 0.0.0.0:8000
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True
